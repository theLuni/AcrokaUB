# 🤖 Acroka UserBot v3.0

**Мощный модульный юзербот для Telegram**, созданный на основе библиотеки Telethon. Предоставляет расширенные возможности автоматизации и управления для Telegram-аккаунтов.

## 🏗️ **Архитектура проекта**

### Основные компоненты:

#### **`main.py`** — точка входа
*   Инициализация и запуск бота
*   Проверка зависимостей и структуры проекта
*   Цветной вывод и обработка ошибок
*   Создание необходимых директорий (`source/mods/`, `config/`, `source/backups/`)

#### **`core/modules.py`** — система модулей
*   `ModuleManager` — загрузка, выгрузка и управление модулями
*   `Module` — базовый класс для всех модулей
*   Автоматическая проверка и установка зависимостей
*   Создание резервных копий модулей
*   Логирование и обработка ошибок

#### **`core/commands.py`** — основные команды
*   `CoreCommands` — ядро системных команд
*   Регистрация обработчиков команд через декораторы
*   Управление модулями, обновлениями, системные утилиты

## 🚀 **Основные возможности**

### ✅ **Управление модулями**
- **Динамическая загрузка/выгрузка** модулей без перезапуска
- **Автоматическая установка зависимостей** (из комментариев в коде)
- **Резервное копирование** загружаемых модулей
- **Список команд** каждого модуля с описаниями

### ⚙️ **Системные команды**
- `.help` — полная справка по командам
- `.info` — информация о системе и боте
- `.ping` — проверка скорости отклика
- `.update` — автоматическое обновление с GitHub
- `.restart` — перезагрузка бота

### 📦 **Работа с модулями**
- `.lm` — загрузить модуль (ответом на файл .py)
- `.ulm [имя]` — выгрузить модуль
- `.rlm [имя]` — перезагрузить модуль
- `.mlist` — список всех загруженных модулей
- `.dlm [файл.py]` — скачать модуль из репозитория

### 🛠️ **Утилиты**
- `.tr [язык] [текст]` — переводчик (через googletrans)
- `.calc [выражение]` — калькулятор
- `.logs` — получение логов
- `.clean` — очистка кэша
- `.cfg` — управление настройками

## 📁 **Структура проекта**
```
AcrokaUB/
├── main.py              # Точка входа
├── core/
│   ├── modules.py      # Менеджер модулей
│   ├── commands.py     # Основные команды
│   └── bot_manager.py  # Управление ботом (файл недоступен)
├── source/
│   ├── mods/          # Пользовательские модули
│   └── backups/       # Резервные копии
├── config/            # Конфигурационные файлы
├── requirements.txt   # Зависимости
└── userbot.log       # Логи
```

## 🔧 **Технические детали**
- **Язык**: Python 3.7+
- **Библиотека**: Telethon 1.40.0+
- **Поддержка**: Windows, Linux, Android (Termux)
- **Конфигурация**: Файлы в папке `config/`
- **Префикс команд**: Настраиваемый (по умолчанию `.`), хранится в `source/prefix.txt`

## 🎯 **Особенности**
1. **Модульность** — каждый функционал в отдельном модуле
2. **Безопасность** — проверка владельца для всех команд
3. **Отказоустойчивость** — резервные копии и восстановление
4. **Автономность** — автоматические обновления и установка зависимостей
5. **Кроссплатформенность** — работает на разных ОС
**Примечание**: Файл `core/bot_manager.py` недоступен по указанной ссылке, поэтому полная архитектура может быть описана не полностью.

> Бот предназначен для **автоматизации задач в Telegram** с акцентом на безопасность, модульность и простоту расширения функционала через пользовательские модули.
